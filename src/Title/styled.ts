import styled from "@emotion/styled";

export const StyledTitle = styled.h2`
  font-family: sans-serif;
  font-weight: 600;
  letter-spacing: 3px;
  font-size: 26px;
  line-height: 25px;
  margin-bottom: 15px;
`;
